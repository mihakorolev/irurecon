let config = {
    style: "bold", // This is just an example value
    color: "red",
};

let elementSettings = {};

elementSettings.style = config.style;

console.log(elementSettings); // { style: "bold" }
